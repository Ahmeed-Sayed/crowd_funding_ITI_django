version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DJANGO_SETTINGS_MODULE=project.settings
      - SECRET_KEY= django-insecure-doe5k2rws1t=mj^)(9cod8=erwcmz%@hqw4^-9)u#r0g4nuv(n
      - DEBUG=1
      - DATABASE_NAME=crowdfund_db
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=ahmed123
      - DB_HOST=db
      - DB_PORT=5432
      - EMAIL_FROM=djangoitittest@gmail.com
      - EMAIL_HOST_USER=djangoititest@gmail.com
      - EMAIL_HOST_PASSWORD=ohmwoypoazymxhkf
      - EMAIL_PORT=587
      - EMAIL_USE_TLS=True

  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust

volumes:
  postgres_data:
